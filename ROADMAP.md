# ROADMAP — 距离「婚礼现场敢用」还差什么

> 生成于 2026-08-30。当前状态：功能完整、双端跑通、自动化测试全绿，
> 但只在小规模模拟下验证过。本文按「婚礼当天会不会翻车」排序缺口。

---

## 一、现状快照

| 能力 | 状态 |
|---|---|
| 四大功能（签到墙/祝福弹幕/抽奖/摇一摇） | ✅ 完成，22+10 项 e2e 断言全过 |
| 单机模式（BroadcastChannel） | ✅ 完成 |
| 真机跨设备（WebSocket） | ⚠️ 10 人以下没问题，**规模上去会崩**（见 P0-1） |
| QR 码 | ✅ 与 Python 库 48/48 矩阵一致 |
| 小程序套壳 | ✅ 代码就绪，缺 HTTPS 域名与业务域名配置 |
| 生产部署 | ❌ 无部署脚本、无 HTTPS 方案、无进程守护 |

---

## 二、P0 — 婚礼当天会翻车的

> ✅ 更新于 2026-08-30：P0-1 / P0-2 / P0-4 已实现并通过压测与回归，P0-3 仍需真机配合。

### P0-1 全量 state 广播打爆局域网 ✅ 已修复

**原机理**：`server.js` 每收到一条 state 就把**完整 state** 广播给所有客户端。
200 宾客含 base64 头像时单份 state ≈ 1MB，摇一摇 50 人 = 400MB/s 出站，打爆千兆局域网。

**修复**（`server.js` + `remote-store.js`）：
1. 头像移出 state：`POST /upload` 落盘 `data/avatars/`，state 只存 `/avatars/<id>.jpg`；
   200 人 state 体积 1MB+ → **15.6KB**（缩小 65 倍）
2. 摇分轻量协议：`{type:'score', guestId, delta}` 上报，服务端聚合每 200ms
   广播一次 `{type:'scores', scores}` 纯数字 map
3. state 广播 100ms 合并，窗口内只发最新版

**验收（`tools/verify/load.js`，2026-08-30 实测）**：
- 100 客户端 × 8 条/秒 × 15 秒 = 11300 条上报 → 大屏只收到 **75 条**聚合广播（150:1 压缩）
- 大屏吞吐 **0.02MB 总量（~1.3KB/s）**，远低于 10MB/s 验收线（修复前同场景 400MB/s）
- 单条消息最大 2.1KB，0 客户端错误

### P0-2 服务端零持久化，重启即丢全部数据 ✅ 已修复

**修复**（`server.js`）：收到 state 后 500ms 防抖写盘 `data/state.json`（含 scores 与 seq），
启动时自动加载；`reset` 指令同时删除持久化文件；头像存 `data/avatars/`。

**验收（2026-08-30 实测）**：签到 5 人 + 抽奖 5 条 → kill 进程 → 重启 →
pull 恢复宾客 5 / 中奖 5 / seq 14，头像文件亦在。

### P0-4 主持手机控台 ✅ 已实现

`host.html` + `js/host.js`：主持人手机扫码（大屏右上角有专用二维码）即可：
- 切换大屏舞台（签到墙 / 抽奖 / 摇一摇）
- 抽奖：选奖项、开始/停止滚动、重抽本轮
- 游戏：开始（30 秒）、重置
- 弹幕开关（已纳入共享 state `config.danmaku`）

实现方式：抽奖控制（`state.lottery`）与弹幕开关（`state.config.danmaku`）全部
下沉到共享 state，大屏订阅即跟随，不引入新协议。

**验收（`tools/verify/smoke-host.js`，10/10 通过）**：遥控切舞台 / 开始·停止抽奖
产生中奖者 / 开始游戏进入倒计时 / 弹幕开关，全部实时反映到大屏。

### P0-3 真机浏览器未实测 ⏳ 待人工配合
目前所有验证都在桌面 Chromium 里。现场主力是**微信内置浏览器**，必须真机过一遍：

- [ ] 微信内打开：签到、发祝福、摇一摇（微信 XWeb 内核对 devicemotion 的策略）
- [ ] iOS Safari：陀螺仪授权弹窗流程（HTTPS 下）
- [ ] 安卓各厂商浏览器：全屏 API、字体渲染
- [ ] 弱网 / 切后台回前台：state 能否恢复、身份是否保持
- [ ] 微信内长按识别二维码 vs 相机扫码，两条路径都要通

**验收**：至少 3 台不同品牌真机全流程跑通并录屏留档。

### P0-4 主持人手机控台缺失

**机理**：抽奖、切舞台、开始游戏都得在大屏电脑上点。但主持人在舞台上，
不可能跑回电脑。这是现场操作流程上的**硬缺口**。

**方案**：mobile.html 增加 `?host=1` 主持模式（或独立 host.html）：
- 三个舞台切换按钮 + 抽奖开始/停止 + 游戏开始
- 复用现有 ws 通道，大屏端已经监听 stage 变化（`A.setStage` 已实现），改动很小
- 主持人手机与签到手机用不同二维码进入（大屏设置里出第二个二维码）

**工作量**：约半天，性价比最高的 P0。

---

## 三、P1 — 体验与运维

### P1-1 婚礼视觉定制

现在的颜值全靠 CSS 渐变，缺婚礼的灵魂——**新人的照片**。

- 设置里支持上传：背景图、新人合照、婚礼 logo（同样走 P0-1 的上传通道）
- 大屏标题区显示合照，背景图加暗化遮罩保证文字可读
- 附赠：倒计时页（仪式开始前滚动播放）

### P1-2 背景音乐

大屏端音乐播放器（上传 mp3 → 循环播放）。注意浏览器自动播放策略：
首次用户点击「开始」时初始化 AudioContext，否则会被静音。

### P1-3 数据留存与导出

婚礼后的祝福语是纪念品。三个出口：
- 导出 JSON（含全部签到/祝福/中奖记录）
- 祝福语生成纪念长图（canvas 绘制，可打印）
- 自动定时备份到服务器磁盘（配合 P0-2）

### P1-4 服务端鉴权（轻量）

现在任何知道地址的人都能发 state 重置现场。加一个现场口令：
设置页生成 4 位数字口令，host 模式和写操作需携带。签到端只读+有限写（签到/祝福/摇分），不做重置类操作。

### P1-5 部署脚本

- `deploy.bat` / `deploy.sh`：pm2 启动 + 开机自启
- 一页「婚礼当天操作手册」（打印出来给婚礼策划或伴郎：先开什么、出问题按哪个键）

---

## 四、P2 — 锦上添花

| 项 | 说明 |
|---|---|
| 新人故事时间线 | 大屏轮播「恋爱大事记」，仪式候场时播放 |
| 现场投票 | 「伴娘团哪家强」类接亲游戏，复用现有通道 |
| 红包雨/爱心雨特效 | 抽奖前的暖场动画 |
| 数据大屏页 | 多少人参与、多少祝福，仪式尾声展示 |
| 小程序原生化 | 若 web-view 方案审核受阻，再用 uni-app 重写（工作量 ×3，最后才考虑） |

---

## 五、路线图

```
M1 婚礼可用（P0，约 2-3 天）
├─ Day1  并发改造：头像上送独立接口 + 摇分专用协议 + 广播合并
├─ Day1  服务端持久化（防抖写盘 + 启动加载）
├─ Day2  主持人手机控台（?host=1）
├─ Day2  100 并发压测脚本 + 真机浏览器实测（3 台）
└─ Day3  部署脚本 + 婚礼当天操作手册

M2 体验升级（P1，约 1-2 天）
├─ 视觉定制（照片/背景/logo）
├─ 背景音乐
├─ 数据导出 + 纪念长图
└─ 现场口令鉴权

M3 产品化（P2，按需）
└─ 投票 / 特效 / 故事时间线 / 原生化
```

**关键判断**：如果婚礼日期在两周内，M1 是不可谈判的底线——尤其 P0-1，
不修的话摇一摇环节 30 人以上必卡死；P0-2 不修的话一次意外断电就没有翻盘机会。

---

## 六、婚礼前 72 小时检查清单（补充现有 README）

- [ ] 压测通过：100 模拟客户端并发摇动 < 10MB/s
- [ ] 3 台真机全流程录屏留档
- [ ] 断电演练：拔电源 → 重启 → 数据完整恢复
- [ ] 备用方案就绪：第二台笔记本装好环境、同一份数据备份 U 盘
- [ ] 主持人控台手机提前充满电、锁定亮度
- [ ] 打印版操作手册交到非技术人员手里（伴郎/策划），含「出问题先重启大屏浏览器」

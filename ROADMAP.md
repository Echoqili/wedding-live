# ROADMAP — 距离「婚礼现场敢用」还差什么

> 生成于 2026-08-30。当前状态：功能完整、双端跑通、自动化测试全绿，
> 但只在小规模模拟下验证过。本文按「婚礼当天会不会翻车」排序缺口。

---

## 一、现状快照

| 能力 | 状态 |
|---|---|
| 四大功能（签到墙/祝福弹幕/抽奖/摇一摇） | ✅ 完成，22+10 项 e2e 断言全过 |
| 单机模式（BroadcastChannel） | ✅ 完成 |
| 真机跨设备（WebSocket） | ⚠️ 10 人以下没问题，**规模上去会崩**（见 P0-1） |
| QR 码 | ✅ 与 Python 库 48/48 矩阵一致 |
| 小程序套壳 | ✅ 代码就绪，缺 HTTPS 域名与业务域名配置 |
| 生产部署 | ❌ 无部署脚本、无 HTTPS 方案、无进程守护 |

---

## 二、P0 — 婚礼当天会翻车的

> ✅ 更新于 2026-08-30：P0-1 / P0-2 / P0-4 已实现并通过压测与回归，P0-3 仍需真机配合。

### P0-1 全量 state 广播打爆局域网 ✅ 已修复

**原机理**：`server.js` 每收到一条 state 就把**完整 state** 广播给所有客户端。
200 宾客含 base64 头像时单份 state ≈ 1MB，摇一摇 50 人 = 400MB/s 出站，打爆千兆局域网。

**修复**（`server.js` + `remote-store.js`）：
1. 头像移出 state：`POST /upload` 落盘 `data/avatars/`，state 只存 `/avatars/<id>.jpg`；
   200 人 state 体积 1MB+ → **15.6KB**（缩小 65 倍）
2. 摇分轻量协议：`{type:'score', guestId, delta}` 上报，服务端聚合每 200ms
   广播一次 `{type:'scores', scores}` 纯数字 map
3. state 广播 100ms 合并，窗口内只发最新版

**验收（`tools/verify/load.js`，2026-08-30 实测）**：
- 100 客户端 × 8 条/秒 × 15 秒 = 11300 条上报 → 大屏只收到 **75 条**聚合广播（150:1 压缩）
- 大屏吞吐 **0.02MB 总量（~1.3KB/s）**，远低于 10MB/s 验收线（修复前同场景 400MB/s）
- 单条消息最大 2.1KB，0 客户端错误

### P0-2 服务端零持久化，重启即丢全部数据 ✅ 已修复

**修复**（`server.js`）：收到 state 后 500ms 防抖写盘 `data/state.json`（含 scores 与 seq），
启动时自动加载；`reset` 指令同时删除持久化文件；头像存 `data/avatars/`。

**验收（2026-08-30 实测）**：签到 5 人 + 抽奖 5 条 → kill 进程 → 重启 →
pull 恢复宾客 5 / 中奖 5 / seq 14，头像文件亦在。

### P0-4 主持手机控台 ✅ 已实现

`host.html` + `js/host.js`：主持人手机扫码（大屏右上角有专用二维码）即可：
- 切换大屏舞台（签到墙 / 抽奖 / 摇一摇）
- 抽奖：选奖项、开始/停止滚动、重抽本轮
- 游戏：开始（30 秒）、重置
- 弹幕开关（已纳入共享 state `config.danmaku`）

实现方式：抽奖控制（`state.lottery`）与弹幕开关（`state.config.danmaku`）全部
下沉到共享 state，大屏订阅即跟随，不引入新协议。

**验收（`tools/verify/smoke-host.js`，10/10 通过）**：遥控切舞台 / 开始·停止抽奖
产生中奖者 / 开始游戏进入倒计时 / 弹幕开关，全部实时反映到大屏。

### P0-3 真机浏览器未实测 ⏳ 待人工配合（机器无法替代）

目前所有验证都在桌面 Chromium 里。现场主力是**微信内置浏览器**，必须真机过一遍。

**建议的测试配置**（3 台手机，覆盖三条主流内核）：
| # | 机型/浏览器 | 重点 |
|---|---|---|
| A | 安卓 + 微信内置浏览器（现场主力） | 签到/祝福/摇一摇点击兜底、二维码扫码 |
| B | 安卓 + Chrome（HTTPS 下） | devicemotion 陀螺仪真实摇动 |
| C | iPhone + Safari（HTTPS 下） | 陀螺仪授权弹窗、刘海屏安全区 |

**每条手机必测项**：
- [ ] 扫码打开 mobile.html → 签到成功、头像上墙
- [ ] 发祝福 → 大屏列表 + 弹幕出现
- [ ] 摇一摇：点击大圆能加分；HTTPS 下陀螺仪摇动能加分
- [ ] 切后台 10 秒再回来：身份（我的）保持，无需重签
- [ ] 断 Wi-Fi 再连：重新进入后仍能看到自己已签到
- [ ] 上传照片头像：处理成功、大屏显示正常

**验收**：3 台真机全流程跑通并录屏留档；发现的问题回报后修复。

> 💡 局域网 http 下 iOS 拿不到陀螺仪是**平台限制**（苹果强制 HTTPS），
> 点击兜底通道为唯一方案，属预期行为，不算 bug。

### P0-4 主持人手机控台缺失

**机理**：抽奖、切舞台、开始游戏都得在大屏电脑上点。但主持人在舞台上，
不可能跑回电脑。这是现场操作流程上的**硬缺口**。

**实现**：`host.html` + `host.js` 已完成（见 P0-4 ✅）。**工作量**：已交付。

---

## 三、P1 — 体验与运维

### P1-1 婚礼视觉定制

现在的颜值全靠 CSS 渐变，缺婚礼的灵魂——**新人的照片**。✅ 已实现（M2-1）：

- 设置里支持上传：背景图、新人合照、婚礼 logo（复用 /upload 通道）
- 大屏标题区显示合照（圆形金边相框），背景图带暗化遮罩保证文字可读
- 单机模式自动压缩保存（base64），服务器模式存 data/uploads/

### P1-2 背景音乐 ✅ 已实现（M2-2）

- 设置上传 MP3 → 大屏顶栏 🎵 播放/暂停，单曲循环
- 首次点击按钮即用户手势，规避浏览器自动播放限制
- ⚠ 背景音乐仅在**服务器模式**可用（mp3 不进 localStorage）

### P1-3 数据留存与导出 ✅ 已实现（M2-3）

- 设置 →「导出全部数据 JSON」：完整签到/祝福/中奖/设置
- 设置 →「导出祝福长图 PNG」：canvas 绘制纪念长图（1080 宽，可打印/发朋友圈）
- 服务端自动落盘 data/state.json（P0-2 已覆盖）

### P1-4 服务端鉴权（轻量）✅ 已实现（2026-08-30）

- ⚙ 设置 → 现场口令（4 位数字，留空不启用，保持零门槛）
- 启用后：**主持控台**所有控制操作需验证（首次输入，会话内记住，改口令后需重验）；
  **大屏危险操作**（清空中奖/清空祝福/重置全部）需验证
- 宾客端签到/祝福/摇分不受影响（保持现场零摩擦）
- 验证弹层 UI.askPasscode 全内联样式，三端通用

**验收（smoke-passcode.js 10/10）**：未启用放行 / 启用后弹框 / 错误口令拒绝 /
正确口令放行且会话内免打扰 / 大屏危险操作同样受控。

### P1-5 部署脚本 ✅ 已交付

- `deploy.bat`：Windows 现场一键启动（检查 Node → 装依赖 → 启动服务 → 打印局域网地址 → 开浏览器）
- `deploy.sh`：Linux/pm2 部署（pm2 start + save + startup 开机自启）
- `WEDDING-RUNBOOK.md`：**打印版婚礼当天操作手册**（前一天检查 → 开场 60 分钟启动顺序 → 现场流程时间线 → 故障速查 → 紧急联系）

---

## 四、P2 — 锦上添花

| 项 | 说明 |
|---|---|
| 新人故事时间线 | ✅ 已实现（M3-2）：设置编辑大事记，大屏「恋爱故事」舞台 7s 自动轮播 |
| 现场投票 | ✅ 已实现（M3-1）：控台一键发起预设主题，宾客手机投票/改投，大屏实时柱状图 |
| 红包雨/爱心雨特效 | ✅ 已实现（M3-3）：抽奖面板 💗爱心雨 / 🧧红包雨，canvas 粒子 8s 暖场 |
| 数据大屏页 | 多少人参与、多少祝福，仪式尾声展示（按需） |
| 小程序原生化 | 若 web-view 方案审核受阻，再用 uni-app 重写（工作量 ×3，最后才考虑） |

---

## 五、路线图

```
M1 婚礼可用（P0）✅ 2026-08-30 完成（除真机实测 P0-3 待人工）
├─ 并发改造（头像外置 + 摇分轻量协议 + 广播合并）      ✅ 压测 5/5
├─ 服务端持久化（data/state.json 防抖写盘 + 加载）     ✅ 重启恢复实测
├─ 主持人手机控台（host.html）                        ✅ e2e 10/10
├─ 100 并发压测 + 真机浏览器实测（3 台）               ✅ 压测 / ⏳ 真机待用户
└─ 部署脚本 + 婚礼当天操作手册                          ✅ deploy.bat/sh + RUNBOOK

M2 体验升级（P1）✅ 2026-08-30 完成
├─ 视觉定制（照片/背景/logo）        ✅ M2 功能测试 7/7
├─ 背景音乐                           ✅ 上传+播放链路
├─ 数据导出 + 纪念长图               ✅ JSON + PNG 实测下载
└─ 现场口令鉴权（P1-4）              ✅ 2026-08-30 补充完成（测试 10/10）

M3 产品化（P2）✅ 2026-08-30 完成（M3 功能测试 12/12）
├─ 现场投票（伴娘团/敬酒等预设）     ✅ 控台发起 + 手机投票/改投 + 实时柱状图
├─ 故事时间线                        ✅ 设置编辑 + 大屏自动轮播（7s）
└─ 爱心雨 / 红包雨特效               ✅ canvas 粒子 8s 暖场
```

**当前状态**：M1/M2/M3 + P1-4 全部完成，61 项自动化断言全绿。
剩两件外部事项：① 真机 3 台实测（P0-3，需人手）；② 如需公网/小程序上线则做 HTTPS 部署。

---

## 六、婚礼前 72 小时检查清单（补充现有 README）

- [ ] 压测通过：100 模拟客户端并发摇动 < 10MB/s
- [ ] 3 台真机全流程录屏留档
- [ ] 断电演练：拔电源 → 重启 → 数据完整恢复
- [ ] 备用方案就绪：第二台笔记本装好环境、同一份数据备份 U 盘
- [ ] 主持人控台手机提前充满电、锁定亮度
- [ ] 打印版操作手册交到非技术人员手里（伴郎/策划），含「出问题先重启大屏浏览器」
